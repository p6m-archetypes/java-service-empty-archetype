# Prova acceptance tests: `prova` from the repo root runs the whole suite.
[run]
proofs = ["tests"]

# CI runs the same suite; the suite only renders and inspects files, so it needs no special tooling.
[profiles.ci]
format = "json"
